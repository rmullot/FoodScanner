# Cybersecurity — open follow-ups

Running backlog of still-open security points. Remove an entry the same change it is resolved; append new ones as `### ` blocks (context, affected files, suggested fix).

_No open points._
